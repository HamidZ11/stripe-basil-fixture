{
  "name": "stripe-basil-fixture",
  "version": "1.0.0",
  "private": true,
  "description": "Controlled fixture repository for verifying Patchwork's Stripe Basil impact-assessment rules (Upcoming Invoice API removal, Invoice.subscription removal, Issuing Authorization.status split) against real GitHub.",
  "dependencies": {
    "stripe": "^18.0.0"
  }
}
